<% if @err %>
    alert("エラー発生：字幕が追加できません。コメントが追加できません。");
<% else %>
    $("#commentBody").val("");
    $(".commentTable").append("<%= j(render partial: 'comments/comment', locals: {comment: @comment}) %>")
    $(".close").click();
<% end %>