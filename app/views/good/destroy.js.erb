<% if @err %>
    alert(<%= @message %>);
<% else %>
    $("#project_" + <%= @project.id %>).find(".thumb").html("<%= j render 'projects/good', project: @project, count: @project.good_projects.count.to_s, user: @user %>");
<% end %>